torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0
diffusers==0.36.0
transformers==4.55.2
accelerate
einops
easydict
# Optional: for faster inference/training.
# Install manually when needed: pip install flash-attn --no-build-isolation
numpy==1.26.4
tqdm
imageio[ffmpeg]
websockets
msgpack
opencv-python
matplotlib
ftfy
safetensors
Pillow
PyYAML

scipy
# Post-training extras are intentionally excluded from this base file.
# Install separately when doing post-training:
#   pip install lerobot==0.3.3 wandb --no-deps
